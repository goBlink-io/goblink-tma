import { ChakraProvider } from '@chakra-ui/react';
import { Decorator } from '@storybook/react';
export declare const storybookDecorator: Decorator;
export declare function makeStorybookDecorator(ThemeProvider?: typeof ChakraProvider): Decorator;
