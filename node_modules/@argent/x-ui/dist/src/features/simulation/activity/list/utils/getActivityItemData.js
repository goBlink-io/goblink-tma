import { ITEM_TYPE as i } from "../types.js";
const r = ({ activities: a, itemIndex: t }) => ({
  itemData: a.map((e) => ({
    type: i.ACTIVITY,
    activity: e,
    index: t++
  })),
  itemIndex: t
});
export {
  r as getActivityItemData
};
