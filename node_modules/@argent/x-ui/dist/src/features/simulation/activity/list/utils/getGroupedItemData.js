import { ITEM_TYPE as i } from "../types.js";
import { getActivityItemData as n } from "./getActivityItemData.js";
const u = ({ groupedActivities: r, itemIndex: t, subtitleIndex: e }) => ({
  itemData: Object.entries(r).flatMap(([o, m]) => {
    const c = [
      {
        type: i.SUBTITLE,
        text: o,
        index: e++
      }
    ], a = n({ activities: m, itemIndex: t });
    return t = a.itemIndex, c.concat(a.itemData);
  }),
  itemIndex: t,
  subtitleIndex: e
});
export {
  u as getGroupedItemData
};
