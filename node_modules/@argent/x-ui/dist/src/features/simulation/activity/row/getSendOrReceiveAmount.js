import { isNftActivitySummary as c, isTokenActivitySummary as m } from "@argent/x-shared/simulation";
import { isEmpty as p, groupBy as g, uniq as l } from "lodash-es";
function r(n) {
  if (p(n))
    return null;
  const { token: e, nft: o } = g(n, (t) => t.asset.type), s = l(n.map((t) => t.asset.type)).length > 1, i = n.length;
  if (s || (e == null ? void 0 : e.length) > 1 || (o == null ? void 0 : o.length) > 1)
    return i;
  if ((o == null ? void 0 : o.length) === 1) {
    const [t] = n;
    if (c(t))
      return t.asset.amount || 1;
  }
  if ((e == null ? void 0 : e.length) === 1) {
    const [t] = n;
    if (m(t))
      return t.asset.amount || 0;
  }
}
export {
  r as getSendOrReceiveAmount
};
