const p = (e) => {
  var r, a;
  if (e.details.type === "payment" && e.details.counterparty)
    return e.details.counterparty;
  if (e.details.type === "multicall") {
    const t = (r = e.details.calls) == null ? void 0 : r.find((n) => {
      var l;
      return ((l = n.details) == null ? void 0 : l.type) === "payment";
    });
    if ((t == null ? void 0 : t.details.type) === "payment")
      return (a = t.details) == null ? void 0 : a.counterparty;
  }
};
export {
  p as getCounterparty
};
