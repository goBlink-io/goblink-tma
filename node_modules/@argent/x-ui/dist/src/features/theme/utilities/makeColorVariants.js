import { colord as r, extend as f } from "colord";
import m from "colord/plugins/lch";
import { interpolate as h, linear as x } from "popmotion";
f([m]);
const C = [50, 100, 200, 300, 400, 500, 600, 700, 800, 900];
var o;
(function(t) {
  t.HSL = "HSL", t.HCL = "HCL";
})(o || (o = {}));
const V = (t, i = o.HCL) => {
  const s = {}, c = i === o.HCL ? r(t).toLch() : r(t).toHsl(), { h: a, l: p, a: H } = c, L = h([0, 500, 1e3], [100, p, 0], {
    ease: x
  });
  return C.forEach((n) => {
    if (n === 500)
      s[n] = t;
    else {
      const l = L(n);
      if (i === o.HCL) {
        const { c: e } = c;
        s[n] = r({ h: a, c: e, l, a: H }).toHex();
      } else {
        const { s: e } = c;
        s[n] = r({ h: a, s: e, l, a: H }).toHex();
      }
    }
  }), s;
};
export {
  o as ColorMode,
  V as makeColorVariants
};
