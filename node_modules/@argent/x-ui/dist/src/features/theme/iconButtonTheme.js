import { defineStyleConfig as t } from "@chakra-ui/react";
import { mode as i } from "@chakra-ui/theme-tools";
import { buttonBaseStyle as o } from "./buttonTheme.js";
const d = t({
  baseStyle: (e) => ({
    ...o,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    rounded: "full",
    fontWeight: "bold",
    cursor: "pointer",
    outline: "none",
    border: "none",
    textAlign: "center",
    _disabled: {
      pointerEvents: "none"
    },
    _focusVisible: {
      boxShadow: i("outline-accent", "outline")(e)
    }
  }),
  sizes: {
    sm: {
      width: "8",
      height: "8",
      fontSize: "lg"
    },
    md: {
      width: "10",
      height: "10",
      fontSize: "xl"
    },
    lg: {
      width: "12",
      height: "12",
      fontSize: "2xl"
    }
  },
  variants: {
    solid: (e) => ({
      ...o,
      bg: `${e.colorScheme}.500`,
      color: "white"
    }),
    outline: {
      ...o,
      bg: "transparent",
      border: "1px solid",
      borderColor: "currentColor"
    }
  },
  defaultProps: {
    size: "md",
    variant: "solid",
    colorScheme: "default"
  }
});
export {
  d as iconButtonTheme
};
