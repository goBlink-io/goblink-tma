import { getVoidPromiseResult as s } from "./utils.js";
function o(t) {
  return class extends t {
    constructor({ enabled: e, appVersion: r }) {
      super(), this.enabled = e, this.appVersion = r;
    }
    track(e, r) {
      return this.enabled ? super.isLoaded ? {
        promise: super.track(e, {
          app_version: this.appVersion,
          ...r
        }).promise
      } : s() : s();
    }
  };
}
export {
  o as WebAnalyticsServiceFactory
};
