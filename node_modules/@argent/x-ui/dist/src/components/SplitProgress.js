import { jsx as l } from "react/jsx-runtime";
import { Box as e } from "@chakra-ui/react";
const a = ({ value: s, max: r }) => {
  const t = Math.ceil(r), c = Math.ceil(s / r * t), n = Array.from({ length: t }).map((i, o) => /* @__PURE__ */ l(e, { w: `${100 / t}%`, h: 1, bg: o < c ? "primary.500" : "#803820", borderLeftRadius: o === 0 ? "xl" : "none", borderRightRadius: r - 1 === o ? "xl" : "none", mx: "1px" }, o));
  return /* @__PURE__ */ l(e, { display: "flex", w: "100%", mt: 3, children: n });
};
export {
  a as SplitProgress
};
