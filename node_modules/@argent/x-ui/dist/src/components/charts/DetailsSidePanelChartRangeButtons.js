import { jsx as u } from "react/jsx-runtime";
import { useCallback as c } from "react";
import { useTranslation as h } from "react-i18next";
import { SegmentedButtons as d } from "../SegmentedButtons.js";
const m = (e) => [
  {
    label: e("charts.oneDay"),
    value: "oneDay"
  },
  {
    label: e("charts.oneWeek"),
    value: "oneWeek"
  },
  {
    label: e("charts.oneMonth"),
    value: "oneMonth"
  },
  {
    label: e("charts.oneYear"),
    value: "oneYear"
  },
  {
    label: e("charts.allTime"),
    value: "allTime"
  }
], g = ({ setTimeFrameOption: e, timeFrameOption: o, isUnavailable: n, ...t }) => {
  const { t: s } = h(), l = m(s), r = l.findIndex((a) => a.value === o), i = c((a) => {
    e(l[a].value);
  }, [e, l]);
  return /* @__PURE__ */ u(d, { options: l.map((a) => a.label), defaultSelectedIndex: r, onSelectionChange: i, isDisabled: n, equalWidths: !0, width: "full", ...t });
};
export {
  g as DetailsSidePanelChartRangeButtons
};
