import { groupBy as c, map as i } from "lodash-es";
const m = ({ tokenGraphPrices: t }) => {
  const s = c(t, "time");
  return {
    prices: i(s, (o, u) => {
      const r = o, e = +u;
      if (!(r != null && r[0]) || !(r != null && r[0].value))
        return {
          time: e,
          value: 0
        };
      const n = r[0].value ?? 0;
      return {
        time: e,
        value: n
      };
    })
  };
};
export {
  m as buildTokenPrices
};
