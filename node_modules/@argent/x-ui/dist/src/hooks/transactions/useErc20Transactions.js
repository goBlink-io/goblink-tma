import { useMemo as o } from "react";
const s = (n) => n ? n.filter((r) => r.token.type === "erc20") : [], e = (n) => o(() => s(n), [n]), a = (n) => s(n).length > 0, i = (n) => s(n).some((t) => t.amount > 0n), u = (n) => s(n).some((t) => t.amount < 0n);
export {
  s as getERC20Transactions,
  a as hasERC20Transactions,
  i as hasIncomingERC20Transactions,
  u as hasOutgoingERC20Transactions,
  e as useERC20Transactions
};
