import { isEmpty as h } from "lodash-es";
import { useMemo as g } from "react";
const v = (e) => ({ showBalanceChange: g(() => {
  const f = e == null ? void 0 : e.some((p) => !h(p.transfers)), r = e == null ? void 0 : e.some((p) => !h(p.approvals));
  return e && (f || r);
}, [e]) });
export {
  v as useBalanceChange
};
