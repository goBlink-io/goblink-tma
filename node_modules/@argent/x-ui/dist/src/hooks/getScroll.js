function s(o) {
  if (o instanceof Window) {
    const { scrollX: r, scrollY: n } = o;
    return { scrollTop: n, scrollLeft: r };
  }
  const { scrollTop: l, scrollLeft: c } = o;
  return { scrollTop: l, scrollLeft: c };
}
export {
  s as getScroll
};
