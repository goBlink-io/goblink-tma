function a(n) {
  n = n.replace(/^#/, "");
  let r, l, t;
  if (n.length === 3)
    r = parseInt(n[0] + n[0], 16), l = parseInt(n[1] + n[1], 16), t = parseInt(n[2] + n[2], 16);
  else if (n.length === 6)
    r = parseInt(n.slice(0, 2), 16), l = parseInt(n.slice(2, 4), 16), t = parseInt(n.slice(4, 6), 16);
  else
    throw new Error("Invalid HEX color.");
  return 0.299 * r + 0.587 * l + 0.114 * t < 128;
}
export {
  a as isColorDark
};
