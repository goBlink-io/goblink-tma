const e = "px", o = "em", E = "rem";
function c(t = "") {
  const n = new RegExp(String.raw`-?\d+(?:\.\d+|\d*)`), r = new RegExp(`${e}|${o}|${E}`), i = t.match(new RegExp(`${n.source}(${r.source})`));
  return i == null ? void 0 : i[1];
}
function s(t = "") {
  if (typeof t == "number")
    return `${t}px`;
  const n = c(t);
  if (!n || n === e)
    return t;
  if (n === o || n === E)
    return `${parseFloat(t) * 16}${e}`;
}
function f(t = "", n = 16) {
  const r = c(t);
  if (!r || r === o)
    return t;
  if (r === e)
    return `${parseFloat(t) / n}${o}`;
  if (r === E)
    return `${parseFloat(t) * 16 / n}${o}`;
}
function $(t = "") {
  const n = c(t);
  if (!n || n === E)
    return t;
  if (n === o)
    return `${parseFloat(t)}${E}`;
  if (n === e)
    return `${parseFloat(t) / 16}${E}`;
}
export {
  c as getUnit,
  f as toEm,
  s as toPx,
  $ as toRem
};
