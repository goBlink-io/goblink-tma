import { isString as g, upperCase as l } from "lodash-es";
const o = (n) => {
  if (!g(n))
    return;
  const i = /(\d+)$/, e = n.match(i);
  if (e)
    return e[1];
}, p = (n, i = !1) => {
  if (!g(n))
    return "";
  const e = i ? n.replace(/[^0-9a-z ]/gi, "") : n, s = l(e).split(" "), t = o(e), r = s.map((a) => a[0]);
  return t && t.length > 1 && t.length < e.length ? [r[0], t.substring(0, 2)].join("") : s.length === 1 ? s[0].substring(0, 2) : [r[0], r[r.length - 1]].join("");
};
export {
  p as getInitials
};
