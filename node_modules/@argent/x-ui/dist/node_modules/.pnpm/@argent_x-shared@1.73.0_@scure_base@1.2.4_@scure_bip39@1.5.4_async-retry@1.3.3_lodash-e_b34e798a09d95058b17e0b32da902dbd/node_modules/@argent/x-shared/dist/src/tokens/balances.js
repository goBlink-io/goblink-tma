import "lodash-es";
import { num as r, uint256 as e } from "starknet";
import s from "../assets/tokens.json.js";
const l = async (n, a, t) => {
  const o = await t.callContract({
    contractAddress: n,
    entrypoint: "balanceOf",
    calldata: [a]
  });
  return BigInt(r.toHex(e.uint256ToBN({
    low: o[0],
    high: o[1]
  })));
}, m = ({ address: n, networkId: a }) => s.find((t) => t.address === n && t.networkId === a);
export {
  l as getTokenBalance,
  m as useToken
};
