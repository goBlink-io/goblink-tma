import { num as e } from "starknet";
const n = 3, s = process.env.FEE_OVERHEAD ? parseFloat(process.env.FEE_OVERHEAD) : n, t = 10, i = ({ estimatedFee: E, overheadMultiplier: o = s }) => {
  const r = e.toBigInt(E) * BigInt(o * t) / BigInt(t);
  return e.toHex(r);
};
export {
  s as FEE_OVERHEAD,
  i as argentMaxFee
};
