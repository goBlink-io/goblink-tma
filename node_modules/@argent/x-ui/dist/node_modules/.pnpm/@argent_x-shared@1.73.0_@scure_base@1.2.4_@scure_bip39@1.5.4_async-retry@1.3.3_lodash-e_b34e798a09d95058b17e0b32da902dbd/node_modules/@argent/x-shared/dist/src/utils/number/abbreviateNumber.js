import s from "../../../_virtual/numeral.js";
import { isNumeric as m } from "./isNumeric.js";
const f = (t, o = ",", a = 2) => {
  let e = t;
  if (typeof t == "string" && (e = t.replaceAll(o, "")), !m(e))
    return;
  const r = Number(e);
  if (Math.abs(r) < 1e3)
    return r.toString();
  const i = `0.[${"0".repeat(a)}]a`;
  return s(r).format(i).toUpperCase();
};
export {
  f as abbreviateNumber
};
