import o from "url-join";
import { ensureArray as a } from "../arrays.js";
const m = (t, e) => {
  const r = new URLSearchParams(e).toString(), n = r.length ? `?${r}` : "";
  return o(...a(t), n);
};
export {
  m as urlWithQuery
};
