import { isUnlimitedAmount as c } from "./isUnlimitedAmount.js";
import { prettifyTokenNumber as d } from "../utils/number/prettifyNumber.js";
import { isNumeric as T } from "../utils/number/isNumeric.js";
import { formatUnits as y } from "../bigdecimal/formatUnits.js";
const b = "Unlimited", I = ({ amount: t, decimals: m, symbol: l, showPlusSign: a = !1, withSymbol: f = !0, unlimitedText: u = b, prettyConfigOverrides: p }) => {
  if (!T(t))
    return null;
  let e, r = !1;
  if (c(t))
    e = u;
  else {
    const i = Number(m), o = BigInt(t);
    r = o > 0n;
    const s = i > 0 ? y({ value: o, decimals: i }) : o.toString();
    e = i > 0 ? d(s, p) : s;
  }
  const n = [e, f && l].filter(Boolean).join(" ");
  return a && r ? `+${n}` : n;
};
export {
  b as PRETTY_UNLIMITED,
  I as prettifyTokenAmount
};
