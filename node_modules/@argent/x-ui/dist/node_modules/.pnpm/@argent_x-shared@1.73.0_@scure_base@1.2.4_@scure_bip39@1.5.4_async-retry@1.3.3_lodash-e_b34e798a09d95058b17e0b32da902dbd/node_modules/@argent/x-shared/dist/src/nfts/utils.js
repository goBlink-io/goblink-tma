const t = ({ image_uri: r, image_url_copy: i }) => {
  if (r && i) {
    if (!i.startsWith("ipfs://"))
      return i;
    if (!r.startsWith("ipfs://"))
      return r;
  }
  return i || r;
};
export {
  t as getNftPicture
};
