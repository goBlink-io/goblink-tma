import { z as r } from "zod";
const t = r.string().regex(/^(0x)?[0-9a-fA-F]+$/, "Invalid hex string"), x = t.transform((e) => {
  const s = e.startsWith("0x") ? e.slice(2) : e;
  return `0x${s.length % 2 === 0 ? s : `0${s}`}`;
});
export {
  x as hexSchema,
  t as hexSchemaBase
};
