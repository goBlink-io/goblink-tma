function s({ value: r, decimals: e }) {
  if (e === 0)
    return r.toString();
  const i = r < 0n;
  r = i ? -r : r;
  const t = r.toString().padStart(e, "0"), o = t.slice(0, -e) || "0", c = t.slice(-e).replace(/0+$/, "");
  return `${i ? "-" : ""}${o}${c ? `.${c}` : ""}`;
}
export {
  s as formatUnits
};
