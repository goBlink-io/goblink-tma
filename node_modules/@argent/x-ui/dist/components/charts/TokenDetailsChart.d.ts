import { BoxProps } from '@chakra-ui/react';
import { DeepPartial, TimeChartOptions } from 'lightweight-charts';
import { ChartAsset, CrosshairMoveProps, SeriesConfig, TokenGraphData } from './types';
export interface TokenDetailsChartProps extends BoxProps {
    asset?: ChartAsset;
    tokenGraphData: TokenGraphData;
    isLoading?: boolean;
    chartOptions: DeepPartial<TimeChartOptions>;
    getSeriesConfig: (markerColor: string, isHovering?: boolean) => SeriesConfig;
    markerColor: string;
    onCrosshairMove?: (props: CrosshairMoveProps) => void;
}
export declare const TokenDetailsChart: ({ tokenGraphData, isLoading, chartOptions, getSeriesConfig, markerColor, asset, onCrosshairMove, children, ...rest }: TokenDetailsChartProps) => import("react").JSX.Element;
