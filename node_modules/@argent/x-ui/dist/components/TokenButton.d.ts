import { ButtonProps } from '@chakra-ui/react';
import { FC, ReactNode } from 'react';
export interface TokenButtonProps extends ButtonProps {
    name: string;
    tokenTypeIcons?: ReactNode[];
    symbol: string;
    image: string;
    bigFont?: boolean;
    getTokenIconUrl: ({ name, url }: {
        name: string;
        url: string;
    }) => string;
    subtitle?: string | ReactNode;
    hoverButton?: ReactNode;
    optionsButton?: ReactNode;
    valueLabelPrimary?: string | ReactNode;
    valueLabelSecondary?: string | ReactNode;
    isLoading?: boolean;
    showTokenSymbol?: boolean;
    errorMessage?: {
        message: string;
        description: string;
    };
}
declare const TokenButton: FC<TokenButtonProps>;
export { TokenButton };
