import { CenterProps, Button } from '@chakra-ui/react';
import { ComponentProps, FC, ReactNode } from 'react';
export interface EmptyProps extends Omit<CenterProps, "title"> {
    icon?: ReactNode;
    title?: ReactNode;
    subtitle?: ReactNode;
}
export declare const Empty: FC<EmptyProps>;
export declare const EmptyButton: FC<ComponentProps<typeof Button>>;
