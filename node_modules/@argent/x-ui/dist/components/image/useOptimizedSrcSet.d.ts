import { OptimizedImageFormat } from '../../features/providers';
/**
 * returns an array of image optimizer urls suitable for use as an image srcset property
 *
 * @example
 * ```ts
 * const srcSet = useOptimizedSrcSet(url)
 * // returns ["base-url 480w", "sm-url 720w", ...]
 * return (
 *   <Image srcSet={srcSet} />
 * )
 * ```
 */
export declare const useOptimizedSrcSet: (src?: string, format?: OptimizedImageFormat) => string | undefined;
