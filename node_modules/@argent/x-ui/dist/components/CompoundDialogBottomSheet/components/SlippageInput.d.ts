interface SlippageInputProps {
    onSave: (slippage: number) => void;
    value: number;
    options?: {
        multiplier: number;
        min: number;
        max: number;
        step: number;
    };
}
/**
 *
 * @param onSave - Method triggered on saving the slippage setting, it will get slippage argument represented in BSP (basis points -> 1 bps = 0.01%)
 * @param initValue - Initial value of the input in BPS
 * @param options - Optional settings in BPS
 * @constructor
 */
declare function SlippageInput({ onSave, value, options }: SlippageInputProps): import("react").JSX.Element;
export { SlippageInput };
export type { SlippageInputProps };
