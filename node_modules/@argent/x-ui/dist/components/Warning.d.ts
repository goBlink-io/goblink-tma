import { CenterProps } from '@chakra-ui/react';
import { FC, ReactNode } from 'react';
interface WarningProps extends Omit<CenterProps, "title"> {
    icon?: ReactNode;
    title?: ReactNode;
    subtitle?: ReactNode;
}
export declare const Warning: FC<WarningProps>;
export {};
