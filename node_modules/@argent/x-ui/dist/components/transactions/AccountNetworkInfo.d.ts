import { FC } from 'react';
import { PrettyAccountAddressProps } from './PrettyAccountAddress';
interface AccountNetworkInfoProps {
    accountAddress: string;
    accountName?: string;
    networkId: string;
    networkName: string;
    to?: string;
    PrettyAccountAddress?: FC<PrettyAccountAddressProps>;
}
export declare const AccountNetworkInfo: ({ accountName, accountAddress, networkId, networkName, to, PrettyAccountAddress, }: AccountNetworkInfoProps) => import("react").JSX.Element;
export {};
