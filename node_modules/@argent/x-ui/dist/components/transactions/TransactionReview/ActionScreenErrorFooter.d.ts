import { AccordionProps } from '@chakra-ui/react';
import { FC, ReactNode } from 'react';
interface ActionScreenErrorFooterProps extends Omit<AccordionProps, "title"> {
    errorMessage?: string;
    title: ReactNode;
    expandable?: boolean;
}
export declare const ActionScreenErrorFooter: FC<ActionScreenErrorFooterProps>;
export {};
