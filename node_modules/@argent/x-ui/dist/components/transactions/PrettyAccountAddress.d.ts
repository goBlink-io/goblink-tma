import { ComponentProps, FC, ReactNode } from 'react';
import { TokenIcon } from '../TokenIcon';
export interface PrettyAccountAddressProps extends Pick<ComponentProps<typeof TokenIcon>, "size"> {
    accountAddress: string;
    networkId: string;
    accountName?: string;
    fallbackValue?: (accountAddress: string) => ReactNode;
    icon?: boolean;
    bold?: boolean;
}
export declare const PrettyAccountAddress: FC<PrettyAccountAddressProps>;
