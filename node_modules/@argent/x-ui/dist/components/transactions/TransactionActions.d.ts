import { FC } from 'react';
import { Call, TransactionType } from 'starknet';
export interface TransactionActionsProps {
    transactions: Call[];
    type?: TransactionType;
}
export declare const TransactionActions: FC<TransactionActionsProps>;
