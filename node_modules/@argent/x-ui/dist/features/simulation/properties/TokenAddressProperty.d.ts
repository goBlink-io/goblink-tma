import { Property } from '@argent/x-shared/simulation';
import { FlexProps } from '@chakra-ui/react';
import { FC } from 'react';
interface TokenAddressPropertyProps extends Omit<FlexProps, "property"> {
    property: Extract<Property, {
        type: "token_address" | "nft";
    }>;
    networkId: string;
}
export declare const TokenAddressProperty: FC<TokenAddressPropertyProps>;
export {};
