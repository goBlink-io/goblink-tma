import { Property } from '@argent/x-shared/simulation';
import { FC } from 'react';
interface CallDataPropertyProps {
    property: Extract<Property, {
        type: "calldata";
    }>;
}
export declare const CallDataProperty: FC<CallDataPropertyProps>;
export {};
