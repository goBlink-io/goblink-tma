import { Property } from '@argent/x-shared/simulation';
import { FlexProps } from '@chakra-ui/react';
import { FC } from 'react';
interface AddressPropertyProps extends Omit<FlexProps, "property"> {
    property: Extract<Property, {
        type: "address";
    }>;
    networkId: string;
}
export declare const AddressProperty: FC<AddressPropertyProps>;
export {};
