import { Property } from '@argent/x-shared/simulation';
import { FlexProps } from '@chakra-ui/react';
import { FC } from 'react';
import { HideComponentWithChildren } from '../types';
interface AmountPropertyProps extends Omit<FlexProps, "property"> {
    property: Extract<Property, {
        type: "amount";
    }>;
    hideComponent?: HideComponentWithChildren;
}
export declare const AmountProperty: FC<AmountPropertyProps>;
export {};
