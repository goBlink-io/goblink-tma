import { Activity, NativeActivity } from '@argent/x-shared/simulation';
import { FC, ReactNode } from 'react';
interface DappValueProps {
    dapp: Activity["dapp"] | NativeActivity["dapp"];
    suffix?: ReactNode;
}
export declare const DappValue: FC<DappValueProps>;
interface DappIdValueProps {
    fallback: ReactNode;
    dappId: string;
    suffix?: ReactNode;
}
export declare const DappIdValue: FC<DappIdValueProps>;
interface NativeActivityDappValueProps {
    dapp: NativeActivity["dapp"];
    suffix?: ReactNode;
}
export declare const NativeActivityDappValue: FC<NativeActivityDappValueProps>;
export {};
