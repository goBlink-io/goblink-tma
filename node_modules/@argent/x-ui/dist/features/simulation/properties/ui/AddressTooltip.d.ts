import { TooltipProps } from '@chakra-ui/react';
import { FC } from 'react';
interface AddressTooltipProps extends TooltipProps {
    address: string;
    networkId: string;
    isTransaction?: boolean;
}
export declare const AddressTooltip: FC<AddressTooltipProps>;
export {};
