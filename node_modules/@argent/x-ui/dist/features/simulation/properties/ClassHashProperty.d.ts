import { Property } from '@argent/x-shared/simulation';
import { FlexProps } from '@chakra-ui/react';
import { FC } from 'react';
interface ClassHashPropertyProps extends Omit<FlexProps, "property"> {
    property: Extract<Property, {
        type: "class_hash";
    }>;
    networkId: string;
}
export declare const ClassHashProperty: FC<ClassHashPropertyProps>;
export {};
