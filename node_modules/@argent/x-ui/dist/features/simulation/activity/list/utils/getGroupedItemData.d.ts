import { AnyActivity } from '@argent/x-shared/simulation';
import { ItemData } from '../types';
export declare const getGroupedItemData: ({ groupedActivities, itemIndex, subtitleIndex, }: {
    groupedActivities: Record<string, AnyActivity[]>;
    itemIndex: number;
    subtitleIndex: number;
}) => {
    itemData: ItemData[];
    itemIndex: number;
    subtitleIndex: number;
};
