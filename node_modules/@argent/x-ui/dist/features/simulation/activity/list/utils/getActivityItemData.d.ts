import { AnyActivity } from '@argent/x-shared/simulation';
import { ItemData } from '../types';
export declare const getActivityItemData: ({ activities, itemIndex, }: {
    activities: AnyActivity[];
    itemIndex: number;
}) => {
    itemData: ItemData[];
    itemIndex: number;
};
