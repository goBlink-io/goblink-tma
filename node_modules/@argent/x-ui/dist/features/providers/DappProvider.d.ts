import { KnownDapp } from '@argent/x-shared';
import { FC, PropsWithChildren } from 'react';
export type UseDappId = (dappId: string) => KnownDapp | undefined;
interface DappContextProps {
    useDappId: UseDappId;
}
export declare const useDappId: UseDappId;
export interface DappProviderProps extends PropsWithChildren, DappContextProps {
}
export declare const DappProvider: FC<DappProviderProps>;
export {};
