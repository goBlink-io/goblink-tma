import { ModalWallet } from '../../../../types/modal';

export interface RequestArguments {
    method: string;
    params?: unknown[] | object;
}
type ModalWalletExtended = ModalWallet & {
    dappName: string;
};
declare class ArgentModal {
    bridgeUrl: string;
    mobileUrl: string;
    type: "overlay" | "window";
    wcUri?: string;
    private overlay?;
    private popupWindow?;
    private closingTimeout?;
    private standaloneConnectorModal?;
    showWalletConnectModal(wcUri: string, modalWallet: ModalWalletExtended): void;
    getWalletConnectQR(wcUri: string): void;
    private openMobileApp;
    private getQR;
    showApprovalModal(_: RequestArguments): void;
    closeModal({ success, isRequest, }?: Partial<{
        success: boolean;
        isRequest: boolean;
    }>): void;
    private getModal;
    private showModal;
}
export declare const argentModal: ArgentModal;
export {};
