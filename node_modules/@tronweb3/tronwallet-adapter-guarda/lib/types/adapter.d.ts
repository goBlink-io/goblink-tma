import { Adapter, AdapterState, WalletReadyState } from '@tronweb3/tronwallet-abstract-adapter';
import type { Transaction, SignedTransaction, AdapterName, BaseAdapterConfig, Network, TronWeb } from '@tronweb3/tronwallet-abstract-adapter';
import type { Tron } from '@tronweb3/tronwallet-adapter-tronlink';
export interface GuardaAdapterConfig extends BaseAdapterConfig {
    checkTimeout?: number;
    openUrlWhenWalletNotFound?: boolean;
}
export declare const GuardaAdapterName: AdapterName<"Guarda">;
export interface GuardaWallet {
    ready: boolean;
    tronWeb: TronWeb;
    tron: Tron;
}
declare global {
    interface Window {
        guarda?: GuardaWallet;
    }
}
export declare class GuardaAdapter extends Adapter {
    name: AdapterName<"Guarda">;
    url: string;
    icon: string;
    config: Required<GuardaAdapterConfig>;
    private _readyState;
    private _state;
    private _connecting;
    private _wallet;
    private _address;
    constructor(config?: GuardaAdapterConfig);
    get address(): string | null;
    get state(): AdapterState;
    get readyState(): WalletReadyState;
    get connecting(): boolean;
    network(): Promise<Network>;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    signTransaction(transaction: Transaction): Promise<SignedTransaction>;
    signMessage(message: string): Promise<string>;
    private checkAndGetWallet;
    private _checkPromise;
    private _checkWallet;
    private _updateWallet;
    private setAddress;
    private setState;
}
//# sourceMappingURL=adapter.d.ts.map