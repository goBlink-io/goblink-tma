import { Adapter, AdapterState, WalletReadyState, type Network } from '@tronweb3/tronwallet-abstract-adapter';
import type { Transaction, SignedTransaction, AdapterName, BaseAdapterConfig } from '@tronweb3/tronwallet-abstract-adapter';
declare global {
    interface Window {
        binancew3w?: {
            tron: any;
        };
        isBinance: boolean;
    }
}
export interface BinanceWalletAdapterConfig extends BaseAdapterConfig {
    /**
     * Timeout in millisecond for checking if Binance Wallet exists.
     * Default is 2 * 1000ms
     */
    checkTimeout?: number;
}
export declare const BinanceWalletAdapterName: AdapterName<"Binance Wallet">;
export declare class BinanceWalletAdapter extends Adapter {
    name: AdapterName<"Binance Wallet">;
    url: string;
    icon: string;
    config: Required<BinanceWalletAdapterConfig>;
    private _readyState;
    private _state;
    private _connecting;
    private _provider;
    private _address;
    constructor(config?: BinanceWalletAdapterConfig);
    get address(): string | null;
    get state(): AdapterState;
    get readyState(): WalletReadyState;
    get connecting(): boolean;
    /**
     * Get network information used by Binance Wallet.
     * @returns {Network} Current network information.
     */
    network(): Promise<Network>;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    signMessage(message: string): Promise<string>;
    signTransaction(transaction: Transaction): Promise<SignedTransaction>;
    private _onAccountsChanged;
    private _listenEvent;
    private _stopListenEvent;
    private _checkPromise;
    private _checkWallet;
    private _updateProvider;
    private setAddress;
    private setState;
}
//# sourceMappingURL=adapter.d.ts.map