"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.MetaMaskAdapter = exports.MetaMaskAdapterName = void 0;
const multichain_api_client_1 = require("@metamask/multichain-api-client");
const tronwallet_abstract_adapter_1 = require("@tronweb3/tronwallet-abstract-adapter");
const types_js_1 = require("./types.js");
const utils_js_1 = require("./utils.js");
exports.MetaMaskAdapterName = 'MetaMask';
class MetaMaskAdapter extends tronwallet_abstract_adapter_1.Adapter {
    /**
     * Creates an instance of MetaMaskAdapter.
     * @param config - Configuration options for the adapter.
     */
    constructor() {
        super();
        this.name = exports.MetaMaskAdapterName;
        // @prettier-ignore
        this.icon = 'data:image/svg+xml;base64,';
        this.url = 'https://metamask.io';
        this._readyState = tronwallet_abstract_adapter_1.WalletReadyState.Loading;
        this._state = tronwallet_abstract_adapter_1.AdapterState.Disconnect;
        this._connecting = false;
        this._switchingChain = false;
        this._address = null;
        this._transport = (0, multichain_api_client_1.getDefaultTransport)();
        this._client = (0, multichain_api_client_1.getMultichainClient)({ transport: this._transport });
        this._checkWalletPromise = this.checkWallet();
        this._selectedAddressOnPageLoadPromise = this.getInitialSelectedAddress();
        // Auto-restore session on page refresh
        this._checkWalletPromise.then((walletReady) => {
            if (walletReady) {
                this.tryRestoringSession()
                    .then(() => {
                    if (this.address) {
                        this.startListeners();
                        this.setState(tronwallet_abstract_adapter_1.AdapterState.Connected);
                        this.emit('connect', this.address);
                    }
                })
                    .catch((error) => {
                    console.warn('Failed to auto-restore session:', error);
                });
            }
        });
    }
    /** Gets the current connected address. */
    get address() {
        return this._address;
    }
    /** Gets the current state of the adapter. */
    get state() {
        return this._state;
    }
    /** Gets the ready state of the wallet. */
    get readyState() {
        return this._readyState;
    }
    /** Gets whether the adapter is currently connecting. */
    get connecting() {
        return this._connecting;
    }
    /**
     * Connects to the MetaMask wallet.
     * @returns A promise that resolves when connected.
     */
    connect() {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (this.connected || this.connecting) {
                    return;
                }
                if (this._readyState !== tronwallet_abstract_adapter_1.WalletReadyState.Found) {
                    throw new tronwallet_abstract_adapter_1.WalletConnectionError('Wallet not found or not ready');
                }
                const walletReady = yield this._checkWalletPromise;
                if (!walletReady) {
                    throw new tronwallet_abstract_adapter_1.WalletConnectionError('Wallet not found after initialization');
                }
                this._connecting = true;
                try {
                    // Try restoring session
                    yield this.tryRestoringSession();
                    // Otherwise create a session on Mainnet by default
                    if (!this.address) {
                        yield this.createSession(types_js_1.Scope.MAINNET);
                    }
                    // In case user didn't select any Tron scope/account, return
                    if (!this.address) {
                        return;
                    }
                    this.startListeners();
                    this.setState(tronwallet_abstract_adapter_1.AdapterState.Connected);
                    this.emit('connect', this.address);
                }
                catch (error) {
                    throw new tronwallet_abstract_adapter_1.WalletConnectionError(error === null || error === void 0 ? void 0 : error.message, error);
                }
            }
            catch (error) {
                this.emit('error', error);
                throw error;
            }
            finally {
                this._connecting = false;
            }
        });
    }
    /**
     * Disconnects from the MetaMask wallet.
     * @returns A promise that resolves when disconnected.
     */
    disconnect() {
        return __awaiter(this, void 0, void 0, function* () {
            if (this.state !== tronwallet_abstract_adapter_1.AdapterState.Connected) {
                return;
            }
            this.stopListeners();
            this.setAddress(null);
            this.setScope(undefined, false);
            this.setState(tronwallet_abstract_adapter_1.AdapterState.Disconnect);
            this.emit('disconnect');
            yield this._client.revokeSession({ scopes: [types_js_1.Scope.MAINNET, types_js_1.Scope.NILE, types_js_1.Scope.SHASTA] });
        });
    }
    /**
     * Signs a transaction using the MetaMask wallet.
     * @param transaction - The transaction to sign.
     * @param privateKey - Optional private key (not recommended for production).
     * @returns A promise that resolves to the signed transaction.
     */
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    signTransaction(transaction, _) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a;
            try {
                if (!this._scope) {
                    throw new tronwallet_abstract_adapter_1.WalletDisconnectedError('Wallet not connected');
                }
                const contractType = (_a = transaction.raw_data.contract[0]) === null || _a === void 0 ? void 0 : _a.type;
                if (!contractType) {
                    throw new tronwallet_abstract_adapter_1.WalletSignTransactionError('Transaction contract type is required');
                }
                const result = yield this._client.invokeMethod({
                    scope: this._scope,
                    request: {
                        method: 'signTransaction',
                        params: {
                            address: this._address,
                            transaction: {
                                rawDataHex: transaction.raw_data_hex,
                                type: contractType,
                            },
                        },
                    },
                });
                return Object.assign(Object.assign({}, transaction), { signature: [result.signature] });
            }
            catch (error) {
                if (error instanceof Error || (typeof error === 'object' && error.message)) {
                    throw new tronwallet_abstract_adapter_1.WalletSignTransactionError(error.message, error);
                }
                if (typeof error === 'string') {
                    throw new tronwallet_abstract_adapter_1.WalletSignTransactionError(error, new Error(error));
                }
                throw new tronwallet_abstract_adapter_1.WalletSignTransactionError('Unknown error', error);
            }
        });
    }
    /**
     * Signs a message using the MetaMask wallet.
     * @param message - The message to sign.
     * @param privateKey - Optional private key (not recommended for production).
     * @returns A promise that resolves to the signature.
     */
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    signMessage(message, _) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (!this._scope) {
                    throw new tronwallet_abstract_adapter_1.WalletDisconnectedError('Wallet not connected');
                }
                const base64Message = Buffer.from(message).toString('base64');
                const result = yield this._client.invokeMethod({
                    scope: this._scope,
                    request: {
                        method: 'signMessage',
                        params: { message: base64Message, address: this._address },
                    },
                });
                return result.signature;
            }
            catch (error) {
                if (error instanceof Error || (typeof error === 'object' && error.message)) {
                    throw new tronwallet_abstract_adapter_1.WalletSignMessageError(error.message, error);
                }
                if (typeof error === 'string') {
                    throw new tronwallet_abstract_adapter_1.WalletSignMessageError(error, new Error(error));
                }
                throw new tronwallet_abstract_adapter_1.WalletSignMessageError('Unknown error', error);
            }
        });
    }
    /**
     * Switches the chain for the MetaMask wallet.
     * During the initial connection process by TronWallet, this method can be called multiple times in parallel.
     * And if we call the createSession method multiple times in parallel, it fails.
     * That's why we added a _switchingChain flag to avoid multiple simultaneous calls.
     * @param chainId - The chain ID to switch to.
     */
    switchChain(chainId) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a, _b, _c, _d;
            if (this._switchingChain) {
                return;
            }
            this._switchingChain = true;
            if (!this._scope) {
                this._switchingChain = false;
                throw new tronwallet_abstract_adapter_1.WalletDisconnectedError('Wallet not connected');
            }
            const newScope = (0, utils_js_1.chainIdToScope)(chainId);
            if (newScope === this._scope) {
                // Still emit event to reconciliate divergent states between dapp and adapter
                this.emit('chainChanged', { chainId });
                this._switchingChain = false;
                return;
            }
            let session = yield this._client.getSession();
            let isChainInSession = (_b = (_a = session === null || session === void 0 ? void 0 : session.sessionScopes[newScope]) === null || _a === void 0 ? void 0 : _a.accounts) === null || _b === void 0 ? void 0 : _b.includes(`${newScope}:${this._address}`);
            if (!isChainInSession) {
                // Create session for the new scope
                yield this.createSession(newScope, this.address ? [this.address] : undefined);
                session = yield this._client.getSession();
                isChainInSession = (_d = (_c = session === null || session === void 0 ? void 0 : session.sessionScopes[newScope]) === null || _c === void 0 ? void 0 : _c.accounts) === null || _d === void 0 ? void 0 : _d.includes(`${newScope}:${this._address}`);
                if (!isChainInSession) {
                    this._switchingChain = false;
                    throw new tronwallet_abstract_adapter_1.WalletConnectionError('Failed to switch chain');
                }
            }
            this.setScope(newScope);
            this._switchingChain = false;
        });
    }
    /**
     * Get network information used by MetaMask.
     * @returns {Network} Current network information.
     */
    network() {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (this.state !== tronwallet_abstract_adapter_1.AdapterState.Connected || !this._scope) {
                    throw new tronwallet_abstract_adapter_1.WalletDisconnectedError('Wallet not connected');
                }
                const chainId = (0, utils_js_1.scopeToChainId)(this._scope);
                const networkType = (0, utils_js_1.scopeToNetworkType)(this._scope);
                return {
                    networkType,
                    chainId,
                    fullNode: '',
                    solidityNode: '',
                    eventServer: '',
                };
            }
            catch (e) {
                this.emit('error', e);
                throw e;
            }
        });
    }
    /**
     * Listen for up to 2 seconds to the accountsChanged event emitted on page load.
     * @returns If any, the initial selected address.
     */
    getInitialSelectedAddress() {
        return new Promise((resolve) => {
            const timeout = setTimeout(() => {
                resolve(undefined);
            }, 2000);
            const handleAccountChange = (data) => {
                var _a, _b, _c;
                if ((0, utils_js_1.isAccountChangedEvent)(data)) {
                    const address = (_c = (_b = (_a = data === null || data === void 0 ? void 0 : data.params) === null || _a === void 0 ? void 0 : _a.notification) === null || _b === void 0 ? void 0 : _b.params) === null || _c === void 0 ? void 0 : _c[0];
                    if (address) {
                        clearTimeout(timeout);
                        removeNotification === null || removeNotification === void 0 ? void 0 : removeNotification();
                        resolve(address);
                    }
                }
            };
            const removeNotification = this._client.onNotification(handleAccountChange);
        });
    }
    /**
     * Checks if the MetaMask wallet is available in the browser.
     * By default, the _readyState is set to Found to avoid issues on page reloads.
     * But if the wallet is not actually available, we need to update the _readyState accordingly.
     * Average time for wallet to be available is around 50ms.
     * @returns A promise that resolves to true if the wallet is found.
     */
    checkWallet() {
        return __awaiter(this, void 0, void 0, function* () {
            const metamaskInstalled = yield (0, multichain_api_client_1.isMetamaskInstalled)();
            if (metamaskInstalled) {
                this._readyState = tronwallet_abstract_adapter_1.WalletReadyState.Found;
                this.emit('readyStateChanged', this.readyState);
                return true;
            }
            this._readyState = tronwallet_abstract_adapter_1.WalletReadyState.NotFound;
            this.emit('readyStateChanged', this.readyState);
            return false;
        });
    }
    /**
     * Tries to restore an existing session.
     * @returns A promise that resolves when the session is restored or not.
     */
    tryRestoringSession() {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const existingSession = yield this._client.getSession();
                if (!existingSession) {
                    return;
                }
                // Get the address from accountChanged emitted on page load, if any
                const address = yield this._selectedAddressOnPageLoadPromise;
                const scope = this.restoreScope();
                this.updateSession(existingSession, scope, address);
            }
            catch (error) {
                console.warn(`Error restoring session`, error);
            }
        });
    }
    /**
     * Creates a session for the specified scope.
     * @param scope - The TronScope to create the session for.
     * @param addresses - Optional list of addresses to include in the session.
     */
    createSession(scope, addresses) {
        return __awaiter(this, void 0, void 0, function* () {
            let resolvePromise;
            const waitForAccountChangedPromise = new Promise((resolve) => {
                resolvePromise = resolve;
            });
            // If there are multiple accounts, wait for the first accountChanged event to know which one to use
            const handleAccountChange = (data) => {
                var _a, _b, _c;
                if (!(0, utils_js_1.isAccountChangedEvent)(data)) {
                    return;
                }
                const selectedAddress = (_c = (_b = (_a = data === null || data === void 0 ? void 0 : data.params) === null || _a === void 0 ? void 0 : _a.notification) === null || _b === void 0 ? void 0 : _b.params) === null || _c === void 0 ? void 0 : _c[0];
                if (selectedAddress) {
                    removeNotification();
                    resolvePromise(selectedAddress);
                }
            };
            const removeNotification = this._client.onNotification(handleAccountChange);
            const session = yield this._client.createSession({
                optionalScopes: {
                    [scope]: {
                        accounts: (addresses ? addresses.map((addr) => `${scope}:${addr}`) : []),
                        methods: [],
                        notifications: [],
                    },
                },
                sessionProperties: {
                    tron_accountChanged_notifications: true,
                },
            });
            // Wait for the accountChanged event to know which one to use, timeout after 2000ms
            const selectedAddress = yield Promise.race([
                waitForAccountChangedPromise,
                new Promise((resolve) => setTimeout(() => resolve(undefined), 2000)),
            ]);
            this.updateSession(session, undefined, selectedAddress);
        });
    }
    /**
     * Updates the session and the address to connect to.
     * This method handles the logic for selecting the appropriate Tron network scope
     * and address to connect to based on the following priority:
     * 1. First tries to find an available scope in order: previously selected scope > mainnet > shasta > nile
     * 2. For address selection:
     *    - First tries to use the selectedAddress param, most likely coming from
     *      the accountsChanged event
     *    - Falls back to the previously saved address if it exists in the scope
     *    - Finally defaults to the first address in the scope
     *
     * @param session - The session data containing available scopes and accounts
     * @param selectedAddress - The address that was selected by the user, if any
     */
    updateSession(session, selectedScope, selectedAddress) {
        var _a;
        const currentScope = this._scope;
        const scope = this.selectScopeFromSessionWithPriority(session, selectedScope);
        // If no scope is available, don't disconnect so that we can create/update a new session
        if (!scope) {
            this.setAddress(null);
            return;
        }
        const scopeAccounts = (_a = session === null || session === void 0 ? void 0 : session.sessionScopes[scope]) === null || _a === void 0 ? void 0 : _a.accounts;
        // In case the Tron scope is available but without any accounts
        // Could happen if the user already created a session using ethereum injected provider for example or the SDK
        // Don't disconnect so that we can create/update a new session
        if (!(scopeAccounts === null || scopeAccounts === void 0 ? void 0 : scopeAccounts[0])) {
            this.setAddress(null);
            return;
        }
        let addressToConnect;
        // Try to use selectedAddress
        if (selectedAddress && scopeAccounts.includes(`${scope}:${selectedAddress}`)) {
            addressToConnect = selectedAddress;
        }
        // Otherwise try to use the previously saved address in this._address
        else if (this._address && scopeAccounts.includes(`${scope}:${this._address}`)) {
            addressToConnect = this._address;
        }
        // Otherwise select first address
        else {
            addressToConnect = (0, utils_js_1.getAddressFromCaipAccountId)(scopeAccounts[0]);
        }
        // Update the address and scope
        this.setAddress(addressToConnect);
        this.setScope(scope, currentScope !== scope);
    }
    /**
     * Starts listening to the accountsChanged event.
     * @param handler Optional custom handler for the event.
     */
    startListeners(handler) {
        this._removeAccountsChangedListener = this._client.onNotification(handler !== null && handler !== void 0 ? handler : this.handleEvents.bind(this));
    }
    /**
     * Stops listening to the accountsChanged event.
     */
    stopListeners() {
        var _a;
        (_a = this._removeAccountsChangedListener) === null || _a === void 0 ? void 0 : _a.call(this);
        this._removeAccountsChangedListener = undefined;
    }
    /**
     * Handles the accountsChanged event.
     * @param data - The event data
     */
    handleEvents(data) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a, _b, _c, _d, _e, _f;
            if ((0, utils_js_1.isAccountChangedEvent)(data)) {
                const newAddressSelected = (_c = (_b = (_a = data === null || data === void 0 ? void 0 : data.params) === null || _a === void 0 ? void 0 : _a.notification) === null || _b === void 0 ? void 0 : _b.params) === null || _c === void 0 ? void 0 : _c[0];
                if (!newAddressSelected) {
                    // Disconnect if no address selected
                    yield this.disconnect();
                    return;
                }
                const session = yield this._client.getSession();
                if (!session) {
                    return;
                }
                this.updateSession(session, this._scope, newAddressSelected);
            }
            else if ((0, utils_js_1.isSessionChangedEvent)(data)) {
                const session = data === null || data === void 0 ? void 0 : data.params;
                if (!session) {
                    return;
                }
                const scope = this.selectScopeFromSessionWithPriority(session);
                if (!scope) {
                    // Disconnect if no scope selected
                    yield this.disconnect();
                    return;
                }
                const isAccountsEmpty = !(((_f = (_e = (_d = session === null || session === void 0 ? void 0 : session.sessionScopes) === null || _d === void 0 ? void 0 : _d[scope]) === null || _e === void 0 ? void 0 : _e.accounts) === null || _f === void 0 ? void 0 : _f.length) > 0);
                if (isAccountsEmpty) {
                    // Disconnect if no address selected
                    yield this.disconnect();
                    return;
                }
                this.updateSession(session, scope);
            }
        });
    }
    /**
     * Sets the current address.
     * Emits an accountsChanged event if the address changes.
     * @param address - The address to set, or null if disconnected.
     */
    setAddress(address) {
        if (this._address === address) {
            return;
        }
        if (address) {
            this.emit('accountsChanged', address, this._address || '');
        }
        this._address = address;
    }
    /**
     * Sets the adapter state and emits a state change event if necessary.
     * @param state - The new adapter state.
     */
    setState(state) {
        const preState = this.state;
        if (state !== preState) {
            this._state = state;
            this.emit('stateChanged', state);
        }
    }
    /**
     * Sets the current scope.
     * @param scope - The new scope.
     */
    setScope(scope, emitChainChanged = true) {
        if (this._scope === scope) {
            return;
        }
        localStorage.setItem('metamaskAdapterScope', scope !== null && scope !== void 0 ? scope : '');
        this._scope = scope;
        if (!this._scope) {
            return;
        }
        if (emitChainChanged) {
            const newChainId = (0, utils_js_1.scopeToChainId)(this._scope);
            this.emit('chainChanged', { chainId: newChainId });
        }
    }
    /**
     * Restores the scope from local storage.
     * @returns The restored scope, or undefined if not found.
     */
    restoreScope() {
        const scope = localStorage.getItem('metamaskAdapterScope');
        return scope ? scope : undefined;
    }
    /**
     * Selects the scope from the session with priority order: mainnet > shasta > nile
     * @param session - The session data containing available scopes
     * @returns The selected scope, or undefined if no scope is available
     */
    selectScopeFromSessionWithPriority(session, selectedScope) {
        var _a;
        const sessionScopes = new Set(Object.keys((_a = session === null || session === void 0 ? void 0 : session.sessionScopes) !== null && _a !== void 0 ? _a : {}));
        const scopePriorityOrder = (selectedScope ? [selectedScope] : []).concat([
            types_js_1.Scope.MAINNET,
            types_js_1.Scope.SHASTA,
            types_js_1.Scope.NILE,
        ]);
        return scopePriorityOrder.find((scope) => sessionScopes.has(scope));
    }
}
exports.MetaMaskAdapter = MetaMaskAdapter;
//# sourceMappingURL=adapter.js.map